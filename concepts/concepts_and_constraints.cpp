#include <catch2/catch_test_macros.hpp>
#include <iostream>
#include <map>
#include <string>
#include <vector>

using namespace std::literals;
TEST_CASE("constraints")
{
    REQUIRE(true);
}

TEST_CASE("concepts")
{
    REQUIRE(true);
}